# Image name to tag locally and optionally push to Docker Hub
APP_IMAGE=mukeshkumarjangid/springboot-compose-demo:latest

# Database credentials (dev only – rotate in real environments)
DB_NAME=studentdb
DB_USER=postgres
DB_PASSWORD=root